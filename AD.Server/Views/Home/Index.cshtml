@model HomeViewModel
<div class="topbar">
    <div class="title">
        <h1>AD-WebApp</h1>
        <p>Velkommen tilbage @Model.Name</p>
        @* @if(!string.IsNullOrEmpty(Model.GroupNumber))
        {
            <p>Gruppe nummer: @Model.GroupNumber</p>
        } *@
    </div>
    <div class="logout">
        <a href="/api/auth/logout" title="Log ud">
            <i class="fa-solid fa-right-from-bracket"></i>
            <span class="logout-label">Log ud</span>
        </a>
    </div>
</div>

<div class="grid">
    @foreach (var obj in Model.Shortcuts)
    {
        <a href="@obj.URL" class="link-box">
            <div class="content">
                <i class="@obj.Icon"></i>
                <p>@obj.Title</p>
            </div>
        </a>
    }
</div>